What you discovered.
The `seed.py` file had 0% test coverage and was completely untested.

What you fixed/added.
Created `tests/test_seed.py` with tests for the `seed.py` file. The test cases verify that `seed()` correctly sets up the `admin` user, `General` and `Engineering` workspaces, as well as the two agents `Alice's Claude` and `Bob's Gemini`. I also added a test to ensure `seed()` is idempotent, checking that multiple executions do not create duplicate records. I also fixed an issue with unused variable in `tests/test_encryption.py`.

Next Recommended Target
Further investigation into frontend component test coverage to ensure no regressions occur.