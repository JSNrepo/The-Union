What you discovered:
The pytest test suite lacked test coverage for the rate-limiting on the `/register` endpoint in `backend/app/main.py`. This meant the system was untested for the defense against brute force/DoS attacks caused by excessive calls to the expensive bcrypt hashing endpoint.

What you fixed/added:
Added a new test `test_register_rate_limit` in `backend/tests/test_main.py` which triggers the rate limit logic by sending 11 consecutive requests and verifies the backend correctly responds with a 429 status code and the correct detail message.

Next Recommended Target:
Check test coverage for other authentication-related endpoints like login to ensure robustness.
