What you discovered:
The `MessageInput` component's `Escape` key event handler in `frontend/src/app/page.tsx` lacked test coverage, meaning the logic to blur the input when `Escape` is pressed was not verified by the test suite.

What you fixed/added:
Added a new test in `frontend/src/app/page.test.tsx` (`'blurs message input when Escape is pressed'`) that simulates focusing the input and firing an `Escape` keydown event, then asserts that the input successfully lost focus (`expect(input).not.toHaveFocus()`). This increases the overall test coverage and ensures the keyboard accessibility feature remains functional.

Next Recommended Target:
Consider increasing test coverage for the error boundary or edge cases when API responses return malformed data (like unexpected payload structures in workspaces or agents).
