What you discovered.
While reviewing the test suite results for the backend, I discovered a `DeprecationWarning` related to the `crypt` module during the execution of tests (`tests/test_auth.py`). The warning stated that `'crypt' is deprecated and slated for removal in Python 3.13`. This warning was triggered by the `passlib` dependency used for password hashing (`from passlib.context import CryptContext`).

What you fixed/added.
I replaced the `passlib` dependency with the `bcrypt` library directly to fix the deprecation warning.
- Updated `backend/app/auth.py` to remove `CryptContext` and use `bcrypt.hashpw` and `bcrypt.checkpw` for password hashing and verification.
- Removed `passlib==1.7.4` from `backend/requirements.txt` since the project already included `bcrypt==3.2.2`.
- Verified that the `DeprecationWarning` is gone and the test suite still passes successfully with 100% coverage.

Next Recommended Target
I recommend addressing missing backend Pytest coverage if any new files are introduced or investigating if the frontend requires more robust error boundaries for WebSocket state disconnects.