What you discovered:
The `add_security_headers` middleware function in `backend/app/main.py` was missing explicit return type hints (`-> Response`) and parameter type hints for `call_next`. This violates the strict Pyright type checking requirements documented in the memory, which mandates that all functions explicitly declare type hints.

What you fixed/added:
Added the missing type hints to `add_security_headers` in `backend/app/main.py`. The `call_next` parameter is now typed as `Callable[[Request], Awaitable[Response]]` and the function's return type is explicitly set to `Response`. Also added the required imports `Callable`, `Awaitable` from `typing`, and `Response` from `fastapi`.

Next Recommended Target:
Check if there are manual DOM event listeners attached to the global `document` inside `useEffect` in the React frontend (e.g. `document.addEventListener("keydown", ...)`) and replace them with React's built-in synthetic event props directly on the target element or wrap them in appropriate ref handlers to avoid effect churn.
