What you discovered:
I discovered missing test coverage in `backend/app/main.py` lines 218-219. Specifically, the exception block `except (ValueError, TypeError, AttributeError): return []` within `fetch_agent_infos()` inside `chat_message` lacks coverage when `session_data.get('user_id')` is an invalid UUID string.

What you fixed/added:
I added a new test case `test_chat_message_mention_agent_invalid_user_id` in `backend/tests/test_socket.py`. This test mocks the `sio.get_session` method to return an invalid `user_id` ('invalid-uuid'), ensuring the `uuid.UUID` exception block executes and increases the file coverage to 100%.

Next Recommended Target:
Check frontend tests to verify if test coverage is adequate there as well.
