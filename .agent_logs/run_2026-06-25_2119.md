What you discovered.
The frontend component `frontend/src/app/page.tsx` was lacking test coverage for its websocket messaging logic and UI interactions such as copying messages to the clipboard.

What you fixed/added.
I added tests to `frontend/src/app/page.test.tsx` to simulate socket connections, verify that messages can be sent via the socket, and ensure that received messages render correctly and can be copied to the clipboard.

Next Recommended Target
Ensure comprehensive end-to-end (e2e) tests using Playwright are set up for chat interactions to cover full integration paths.
