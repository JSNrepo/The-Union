What you discovered:
I found that `app.dependency_overrides[get_session] = get_session_override` was set globally at the module level in `backend/tests/test_main.py`. This is an unhandled error/state pollution issue where the FastAPI application's state is modified globally during test collection and execution, which can break other tests that might rely on the original dependency or their own overrides. The codebase memory explicitly warns against this: "When writing FastAPI tests using `pytest`, avoid modifying `app.dependency_overrides` globally at the module level. Doing so without proper cleanup will pollute the application state and break other tests. Instead, manage dependency overrides using `yield` fixtures."

What you fixed/added:
I moved the `app.dependency_overrides[get_session] = get_session_override` assignment inside the `client_fixture` in `backend/tests/test_main.py`. I then used a `yield` pattern so that the override is applied before yielding the `TestClient`, and I added `app.dependency_overrides.clear()` afterward to properly clean up the application state.

Next Recommended Target:
Investigate `frontend/src/app/page.tsx` for missing test coverage or missing type hints, specifically around the WebSocket event handling and API fetching logic, or consider adding end-to-end tests for the chat interface.