What you discovered:
- Missing test coverage for the duplicate username check in the `/register` endpoint of the backend `app/main.py`. The branch where a `400 Bad Request` is raised for an already existing username was not tested.

What you fixed/added:
- Added a new test `test_register_duplicate_username` to `backend/tests/test_main.py` which attempts to register two users with the same username.
- The test verifies that the server correctly rejects the duplicate with a 400 status and the expected "Username already registered" message.

Next Recommended Target:
- Add tests to cover other untested branches in `app/main.py`, such as validation errors (e.g., missing API key, missing/invalid payloads) and error cases when interacting with provider APIs or database components.
